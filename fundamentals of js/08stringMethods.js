// Returns a new iterator object that iterates over the code points of a string.
String.prototype[@@iterator]();

// Creates an HTML <a> (anchor) element with the specified name in the string. (Deprecated)
String.prototype.anchor();

// Returns the Unicode character at the specified position in a string.
String.prototype.at();

// Creates an HTML <big> element with the string as its content. (Deprecated)
String.prototype.big();

// Creates an HTML <blink> element with the string as its content. (Deprecated)
String.prototype.blink();

// Creates an HTML <b> (bold) element with the string as its content. (Deprecated)
String.prototype.bold();

// Returns the character at the specified index in a string.
String.prototype.charAt();

// Returns the Unicode of the character at the specified index.
String.prototype.charCodeAt();

// Returns the Unicode code point value of the character at a given index in a string.
String.prototype.codePointAt();

// Combines two or more strings and returns a new string.
String.prototype.concat();

// Checks if a string ends with a specified substring.
String.prototype.endsWith();

// Creates an HTML <tt> (teletype) element with the string as its content. (Deprecated)
String.prototype.fixed();

// Creates an HTML <font> element with the specified color and the string as its content. (Deprecated)
String.prototype.fontcolor();

// Creates an HTML <font> element with the specified size and the string as its content. (Deprecated)
String.prototype.fontsize();

// Returns a string created by using the specified sequence of Unicode values.
String.fromCharCode();

// Returns a string created by using the specified sequence of code points.
String.fromCodePoint();

// Checks if a string contains a specified substring.
String.prototype.includes();

// Returns the index of the first occurrence of a specified value in a string.
String.prototype.indexOf();

// Checks if a string is a well-formed UTF-16 string.
String.prototype.isWellFormed();

// Creates an HTML <i> (italics) element with the string as its content. (Deprecated)
String.prototype.italics();

// Returns the index of the last occurrence of a specified value in a string.
String.prototype.lastIndexOf();

// Creates an HTML <a> (anchor) element with the string as its content and a specified URL. (Deprecated)
String.prototype.link();

// Compares two strings in the current locale.
String.prototype.localeCompare();

// Searches a string for a specified pattern and returns the first match.
String.prototype.match();

// Returns an iterator of all matches for a specified pattern in a string.
String.prototype.matchAll();

// Returns the Unicode Normalization Form of a string.
String.prototype.normalize();

// Pads the end of a string with a specified string or whitespace.
String.prototype.padEnd();

// Pads the start of a string with a specified string or whitespace.
String.prototype.padStart();

// Returns a raw string representation of a template literal.
String.raw();

// Returns a new string repeated a specified number of times.
String.prototype.repeat();

// Replaces a specified value or pattern with another value in a string.
String.prototype.replace();

// Replaces all occurrences of a specified value or pattern with another value in a string.
String.prototype.replaceAll();

// Searches a string for a specified value or pattern and returns the index of the first match.
String.prototype.search();

// Extracts a portion of a string and returns a new string.
String.prototype.slice();

// Creates an HTML <small> element with the string as its content. (Deprecated)
String.prototype.small();

// Splits a string into an array of substrings based on a specified separator.
String.prototype.split();

// Checks if a string starts with a specified substring.
String.prototype.startsWith();

// Creates an HTML <strike> element with the string as its content. (Deprecated)
String.prototype.strike();

// Creates an HTML <sub> (subscript) element with the string as its content. (Deprecated)
String.prototype.sub();

// Returns a portion of a string starting from a specified index. (Deprecated)
String.prototype.substr();

// Returns a portion of a string between two specified indices.
String.prototype.substring();

// Creates an HTML <sup> (superscript) element with the string as its content. (Deprecated)
String.prototype.sup();

// Returns a string with the characters converted to lowercase according to the host's locale.
String.prototype.toLocaleLowerCase();

// Returns a string with the characters converted to uppercase according to the host's locale.
String.prototype.toLocaleUpperCase();

// Returns a new string with all characters converted to lowercase.
String.prototype.toLowerCase();

// Returns the string representation of an object.
String.prototype.toString();

// Returns a new string with all characters converted to uppercase.
String.prototype.toUpperCase();

// Ensures that a string is well-formed UTF-16.
String.prototype.toWellFormed();

// Removes whitespace from both ends of a string.
String.prototype.trim();

// Removes trailing whitespace from the end of a string.
String.prototype.trimEnd();

// Removes leading whitespace from the start of a string.
String.prototype.trimStart();

// Returns the primitive value of a String object.
String.prototype.valueOf();
